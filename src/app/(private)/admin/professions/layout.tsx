import { LayoutPropsType } from "@/shared/types";
import "@/core/app/config/di-container/profession";

export default function ProfessionLayout({ children }: LayoutPropsType) {
  return <>{children}</>;
}
